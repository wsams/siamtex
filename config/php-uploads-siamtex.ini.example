; PHP upload limits for SiamTeX (install into php-fpm conf.d)
;
; Install (Debian/Ubuntu, PHP 8.3 example):
;   sudo cp config/php-uploads-siamtex.ini.example /etc/php/8.3/fpm/conf.d/99-siamtex-uploads.ini
;   sudo systemctl restart php8.3-fpm
;
; SiamTeX allows up to SIAMTEX_MAX_UPLOAD_BYTES (default 5 MB) per file in app logic.
; PHP limits must be at least that high or uploads fail with no file received.

upload_max_filesize = 12M
post_max_size = 16M
max_file_uploads = 40
